---
import { Heading } from '@askoufis/system';
import Base from './Base.astro';

interface Props {
  title: string;
  slug: string;
}

const { title, slug } = Astro.props;
---

<Base title={title}>
  <Heading level={1} id={slug}>{title}</Heading>
  <slot />
</Base>
