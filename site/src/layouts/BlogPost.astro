---
import Base from './Base.astro';

interface Props {
  title: string;
  slug: string;
}

const { title, slug } = Astro.props;
---

<Base title={title}>
  <main>
    <h1 id={slug}>{title}</h1>
    <article>
      <slot />
    </article>
  </main>
</Base>
