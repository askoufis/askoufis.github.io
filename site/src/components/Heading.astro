---
import { Heading as SystemHeading } from '@askoufis/system';

interface Props {
  level: 1 | 2 | 3 | 4 | 5 | 6;
  id: string;
}

const { level, id } = Astro.props;
---

<SystemHeading level={level} id={id}><slot /></SystemHeading>
